# Netlify configuration for Fox Hunt Antiques

[build]
  publish = "."
  functions = "netlify/functions"

# Make the function reachable at /.netlify/functions/fox-brain
[functions]
  node_bundler = "esbuild"
  
